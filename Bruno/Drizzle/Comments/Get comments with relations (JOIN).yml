info:
  name: Get comments with relations (JOIN)
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/comments/with-relations-join
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

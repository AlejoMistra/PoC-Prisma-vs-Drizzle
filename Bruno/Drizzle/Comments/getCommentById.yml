info:
  name: Get comment by id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/comments/{{COMMENT_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
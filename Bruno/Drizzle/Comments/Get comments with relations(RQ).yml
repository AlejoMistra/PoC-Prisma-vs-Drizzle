info:
  name: Get comments with relations(RQ)
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/comments/with-relations-rq
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

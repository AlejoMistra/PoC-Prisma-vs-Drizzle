info:
  name: Get posts with relations (RQ)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/posts/with-relations-rq
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

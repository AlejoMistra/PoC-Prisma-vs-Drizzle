info:
  name: Get all posts
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/posts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
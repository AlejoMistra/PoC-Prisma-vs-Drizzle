info:
  name: Create user
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{DRIZZLE_API_PORT}}/users
  body:
    type: json
    data: |-
      {
        "name": "Tomás Rojas",
        "email": "tomas.rojas+demo@acmeops.io"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

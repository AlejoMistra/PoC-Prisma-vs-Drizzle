info:
  name: Create user
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{DRIZZLE_API_PORT}}/users
  body:
    type: json
    data: |-
      {
        "name": "Alejo Mistraletti",
        "email": "mistraletti@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete user
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:{{DRIZZLE_API_PORT}}/users/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

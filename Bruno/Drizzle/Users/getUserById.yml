info:
  name: Get user by id
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{DRIZZLE_API_PORT}}/users/{{USER_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

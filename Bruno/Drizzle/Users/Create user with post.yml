info:
  name: Create user with post
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:{{DRIZZLE_API_PORT}}/users/with-post
  body:
    type: json
    data: |-
      {
        "name": "Luis Suárez",
        "email": "luis.suarez+demo@acmeops.io",
        "post": {
          "title": "Mi primer post en Drizzle!",
          "content": "User creado desde Bruno para la API de Drizzle"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

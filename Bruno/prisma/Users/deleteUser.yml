info:
  name: Delete user
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:{{PRISMA_API_PORT}}/users/{{USER_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
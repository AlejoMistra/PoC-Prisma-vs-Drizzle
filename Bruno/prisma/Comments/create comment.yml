info:
  name: create comment
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/comments
  body:
    type: json
    data: |-
      {
        "content": "Muy buen post, yo también estoy aprendiendo mucho sobre Prisma!",
        "postId": 1,
        "authorId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

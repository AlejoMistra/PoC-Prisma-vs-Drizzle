info:
  name: create comment
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/comments
  body:
    type: json
    data: |-
      {
        "content": "Prueba",
        "postId": 1,
        "authorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get comments with relations
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{PRISMA_API_PORT}}/comments/with-relations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

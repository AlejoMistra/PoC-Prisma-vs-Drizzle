info:
  name: Create comment
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/comments
  body:
    type: json
    data: |-
      {
              "content": "Muy buena priorización para impacto rápido.",
              "postId": 1,
              "authorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

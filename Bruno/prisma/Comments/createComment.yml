info:
  name: Create comment
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/comments
  body:
    type: json
    data: |2-
       {
              "content": "100prro",
              "postId": 2,
              "authorId": 3
            }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

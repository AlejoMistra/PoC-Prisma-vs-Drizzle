info:
  name: Delete comment
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:{{PRISMA_API_PORT}}/comments/{{COMMENT_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

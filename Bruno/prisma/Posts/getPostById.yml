info:
  name: Get post by id
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{PRISMA_API_PORT}}/posts/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

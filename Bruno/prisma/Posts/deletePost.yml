info:
  name: Delete post
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:{{PRISMA_API_PORT}}/posts/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Create post
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/posts
  body:
    type: json
    data: |2-
       {
              "title": "Boca Juniors",
              "content": "El pibe Janson.",
              "authorId": 3
            }
  auth: inherit

runtime:
  variables:
    - name: USER_ID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

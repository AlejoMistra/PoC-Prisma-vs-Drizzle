info:
  name: Create post
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/posts
  body:
    type: json
    data: |-
      {
        "title": "Post desde Bruno a la API de Prisma",
        "content": "Probando la demo en vivo, aprendimos como funciona Prisma!",
        "authorId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

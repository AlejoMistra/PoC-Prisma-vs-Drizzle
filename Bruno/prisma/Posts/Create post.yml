info:
  name: Create post
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:{{PRISMA_API_PORT}}/posts
  body:
    type: json
    data: |-
      {
        "title": "Prueba",
        "content": "Prueba",
        "authorId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

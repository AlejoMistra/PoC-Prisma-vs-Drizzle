info:
  name: Get posts with relations
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:{{PRISMA_API_PORT}}/posts/with-relations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

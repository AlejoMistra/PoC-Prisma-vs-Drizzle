info:
  name: getAll
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{PRISMA_API_PORT}}/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
